---
const links = [
    { title: 'Home', href: '#inicio' },
    { title: 'Nosotros', href: '#nosotros' },
    { title: 'Cursos', href: '#cursos' },
    { title: 'Contacto', href: '#contacto' },
];
---
<header class="fixed w-full z-50 transition-all duration-300 bg-white/90 backdrop-blur-md shadow-sm">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-26">
            <!-- Logo -->
            <div class="flex-shrink-0 flex items-center">
                <a href="#" class="flex items-center gap-2">
                    <img src="/logo.png" alt="Capacitanorte" class="h-24 w-auto" />
                </a>
            </div>

            <!-- Desktop Menu -->
            <nav class="hidden md:flex space-x-8">
                {links.map(link => (
                    <a href={link.href} class="text-[#099696] hover:text-[#7c629f] px-3 py-2 rounded-md text-lg font-medium transition-colors">
                        {link.title}
                    </a>    
                ))}
            </nav>

            <!-- CTA Button -->
            <div class="hidden md:flex items-center">
                <a href="#contacto" class="bg-[#099696] hover:bg-[#077a7a] text-white px-6 py-2 rounded-full font-bold transition-all shadow-md hover:shadow-lg">
                    Escríbenos
                </a>
            </div>
            
            <!-- Mobile menu button (simple implementation) -->
            <div class="md:hidden flex items-center">
                 <button class="text-gray-700 hover:text-[#7c629f]">
                    <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
                    </svg>
                </button>
            </div>
        </div>
    </div>
</header>
